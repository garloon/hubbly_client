<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hubbly.Mobile.Views.WelcomePage"
             Title="Welcome to Hubbly"
             BackgroundColor="#F8FAFC">

    <Grid>
        <!-- Фоновый градиент -->
        <BoxView>
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#667EEA" Offset="0.0"/>
                    <GradientStop Color="#764BA2" Offset="1.0"/>
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <!-- Основной контент -->
        <ScrollView>
            <VerticalStackLayout Spacing="30" Padding="30" VerticalOptions="Center">

                <!-- Логотип и заголовок -->
                <VerticalStackLayout Spacing="20" HorizontalOptions="Center">
                    <!-- Иконка приложения -->
                    <Frame Padding="20"
                           BackgroundColor="White"
                           HorizontalOptions="Center"
                           HasShadow="True">
                        <Label Text="👋" 
                               FontSize="48"
                               HorizontalOptions="Center"/>
                    </Frame>

                    <!-- Заголовок -->
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Label Text="Добро пожаловать в"
                               FontSize="18"
                               TextColor="White"
                               HorizontalOptions="Center"
                               Opacity="0.9"/>

                        <Label Text="HUBBLY"
                               FontSize="42"
                               FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center"/>

                        <Label Text="Социальный хаб"
                               FontSize="24"
                               TextColor="White"
                               HorizontalOptions="Center"
                               Opacity="0.9"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Описание -->
                <Frame Padding="20"
                       BackgroundColor="#4F46E5"
                       HasShadow="True">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="🌟 Общайтесь в реальном времени"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="White"/>

                        <Label Text="Присоединяйтесь к беседам, знакомьтесь и выражайте себя с помощью 3D-аватаров!"
                               FontSize="14"
                               TextColor="White"
                               Opacity="0.9"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Кнопка входа -->
                <VerticalStackLayout Spacing="15">
                    <Button Text="Войти как гость"
                            Command="{Binding EnterAsGuestCommand}"
                            HeightRequest="60"
                            FontSize="18"
                            FontAttributes="Bold"
                            BackgroundColor="#4F46E5"
                            TextColor="White">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" 
                                         Binding="{Binding IsBusy}" 
                                         Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>

                    <!-- Информация о гостевом режиме -->
                    <Label Text="Никакой регистрации • Ваш гостевой ID создастся автоматически"
                           FontSize="12"
                           TextColor="White"
                           Opacity="0.8"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>

                <!-- Индикатор загрузки -->
                <VerticalStackLayout IsVisible="{Binding IsBusy}" 
                                     Spacing="10"
                                     HorizontalOptions="Center">
                    <ActivityIndicator IsRunning="True"
                                      Color="White"
                                      WidthRequest="40"
                                      HeightRequest="40"/>

                    <Label Text="Creating your space..."
                           FontSize="14"
                           TextColor="White"
                           Opacity="0.9"/>
                </VerticalStackLayout>

                <!-- Футер -->
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="Продолжая, вы соглашаетесь с нашими"
                           FontSize="12"
                           TextColor="White"
                           Opacity="0.7"/>

                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label Text="Условиями использования" 
                               FontSize="12"
                               TextColor="#A5B4FC">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnTermsTapped"/>
                            </Label.GestureRecognizers>
                        </Label>

                        <Label Text="•"
                               FontSize="12"
                               TextColor="White"
                               Opacity="0.7"/>

                        <Label Text="Конфиденциальностью" 
                               FontSize="12"
                               TextColor="#A5B4FC">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnPrivacyTapped"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>